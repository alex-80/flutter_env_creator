name: example
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter_env_creator:
    path: ../

dev_dependencies:
  build_runner: ^2.16.0
  lints: ^4.0.0
